# Skill Checklist

Use this file for shared, platform-neutral checklists.

Example checklist:

- Verify the request actually fits this skill.
- Prefer the shared workflow in `core/guide.md`.
- Read only the references needed for the active variant.
- Avoid platform-specific commands unless the wrapper explicitly allows them.
- Keep outputs concise and actionable.
